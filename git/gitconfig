[user]
        email = troelsmikkelsen@gmail.com
        name = bogeholm
[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
[core]
        editor = vim
